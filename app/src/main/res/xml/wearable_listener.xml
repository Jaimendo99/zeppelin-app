<?xml version="1.0" encoding="utf-8"?>
<wearable-listener
    xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- only MESSAGE_RECEIVED in this example -->
    <intent-filter>
        <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
        <data
            android:scheme="wear"
            android:host="*"
            android:pathPrefix="/" />
    </intent-filter>
    <!-- if you want data events uncomment this
    <intent-filter>
        <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
        <data
            android:scheme="wear"
            android:host="*"
            android:pathPrefix="/" />
    </intent-filter>
    -->
</wearable-listener>